import { Link } from "react-router-dom";

export const Logo = () => {
    return (
        <section className="HeaderLogo">
            <Link to="/feed">AcademiaConnect</Link>
        </section>
    );
};
